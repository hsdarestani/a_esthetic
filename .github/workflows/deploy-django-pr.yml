name: Legacy full-platform deployment (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Production safety notice
        run: |
          echo "Automatic production deployment from pull requests is disabled."
          echo "Use the current main-branch customer-club deployment workflows after validated merges."
